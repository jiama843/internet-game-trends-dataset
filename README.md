# internet-game-trends-dataset
Internet game trends data curation project

The easiest way to run the workflow is by using the notebook `workflow.ipynb`. To get started, install jupyter notebook and run the following in console:

```
jupyter notebook
```

Then from the browser, open `workflow.ipynb`. For additional information, see https://docs.jupyter.org/en/latest/running.html.